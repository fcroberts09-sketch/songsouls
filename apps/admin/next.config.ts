import type { NextConfig } from 'next';

const config: NextConfig = {
  reactStrictMode: true,
  // No third-party analytics or ad SDKs. Ever. See docs/BRIEF.md section 9.7.
};
export default config;
