{
  "name": "@parity/extension",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@parity/parsers": "workspace:*",
    "@parity/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.300",
    "typescript": "^5.6.0"
  }
}
